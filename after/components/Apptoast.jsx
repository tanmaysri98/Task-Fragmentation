import React from "react";

const AppToast = ({ position, message, severity }) => {
  return (
    <div>
      {/* Toast notification UI and functionality here */}
    </div>
  );
};

export default AppToast;
